<!DOCTYPE html>
<html>
<meta name="viewport" content="width=device-width,initial-scale=1.0" />
<head>
  <title>Sgp</title>
  <%= stylesheet_link_tag    'application', media: 'all' %>
  <%= javascript_include_tag 'application' %>
  <%= csrf_meta_tags %>
  <%= stylesheet_link_tag "https://fonts.googleapis.com/icon?family=Material+Icons" %>
  <%= stylesheet_link_tag "https://code.getmdl.io/1.3.0/material.blue-red.min.css" %>
  <%= favicon_link_tag('favicon.ico') %>
  <%= favicon_link_tag('apple-touch-icon-180x180.png',
                     {:rel => 'apple-touch-icon',
                      :sizes => '180x180',
                      :type => 'image/png'}) %>
</head>
<body class='<%= "#{controller.controller_name}" %>'>
  <!-- Always shows a header, even in smaller screens. -->
  <div class="mdl-layout mdl-js-layout mdl-layout--fixed-header">
    <% if user_signed_in? %>
      <header class="mdl-layout__header">
        <div class="mdl-layout__header-row">
          <!-- Title -->
          <span class="mdl-layout-title">S G P　　 C A L E N D A R</span>
          <!-- Add spacer, to align navigation to the right -->
          <div class="mdl-layout-spacer"></div>
          <!-- Navigation. We hide it in small screens. -->
          <nav class="mdl-navigation mdl-layout--large-screen-only">
            <%= link_to calendar_path, class: "mdl-navigation__link", id: "mdl-navigation__link-01" do %>
              <i class="material-icons">event</i>
            <% end %>
            <%= link_to schedules_path, class: "mdl-navigation__link", id: "mdl-navigation__link-01" do %>
              <i class="material-icons">assignment</i>
            <% end %>
            <%= link_to edit_user_registration_path, class: "mdl-navigation__link", id: "mdl-navigation__link-01" do %>
              <i class="material-icons">account_box</i>        
            <% end %>
            <%= link_to "LOGOUT", destroy_user_session_path, method: :delete, class: "mdl-navigation__link", id: "mdl-navigation__link-01" %>          
          </nav>
        </div>
      </header>
      <div class="mdl-layout__drawer">
        <span class="mdl-layout-title">S G P</span>
        <nav class="mdl-navigation">
          <%= link_to calendar_path, class: "mdl-navigation__link" do %>
              <i class="material-icons">event</i>　カレンダー
            <% end %>
            <%= link_to schedules_path, class: "mdl-navigation__link" do %>
              <i class="material-icons">assignment</i>　スケジュール一覧
            <% end %>
            <%= link_to edit_user_registration_path, class: "mdl-navigation__link" do %>
              <i class="material-icons">account_box</i>　ユーザ情報
            <% end %>
          <%= link_to "LOGOUT", destroy_user_session_path, method: :delete, class: "mdl-navigation__link" %>
        </nav>
      </div>
    <% end %>
    <main class="mdl-layout__content">
      <div class="page-content"><!-- Your content goes here --></div>
      <%= yield %>
    </main>
  </div>
</body>
</html>